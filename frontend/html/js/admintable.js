import { getusers } from './script.js';

const admintable = document.getElementById('admintable')

console.log(getusers())

function admintable(admin){
    for (var i = 0; i < item.array.length; i++){
      var list = item.array[i];
      
      list = document.createElement('admintable');
      document.getElementByClass('admintable').appendChild(list);
      
      console.log(getusers());
    }  
}